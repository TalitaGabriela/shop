/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "www.flaticon.com"
        ]
    }
};

export default nextConfig;
